__pycache__/
*.py[cod]
*.egg-info/
.venv/
.idea/
.vscode/
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.log
.DS_Store

# Bind-mounted upload + HLS packaging output. Per-deployment user content,
# never commit — this is what S3 / dual-write are for.
media/

# Keep the directory itself so the bind mount has somewhere to attach.
!media/.gitkeep".claude/"
.claude
