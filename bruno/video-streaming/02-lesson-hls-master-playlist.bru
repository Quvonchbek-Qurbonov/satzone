meta {
  name: Lesson HLS — master playlist
  type: http
  seq: 2
}

get {
  url: {{base_url}}/lessons/{{lesson_id}}/hls/master.m3u8
  auth: bearer
}

query {
  t: {{playback_token}}
}

auth:bearer {
  token: {{access_token}}
}
