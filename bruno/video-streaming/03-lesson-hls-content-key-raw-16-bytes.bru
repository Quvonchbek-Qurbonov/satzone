meta {
  name: Lesson HLS — content key (raw 16 bytes)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/lessons/{{lesson_id}}/hls/key
  auth: bearer
}

query {
  t: {{playback_token}}
}

auth:bearer {
  token: {{access_token}}
}
