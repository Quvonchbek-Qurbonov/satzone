meta {
  name: Lesson playback (capture HLS URL + token)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/lessons/{{lesson_id}}/playback
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
