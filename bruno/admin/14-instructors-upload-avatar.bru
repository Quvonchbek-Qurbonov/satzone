meta {
  name: Instructors — upload avatar
  type: http
  seq: 14
}

post {
  url: {{base_url}}/admin/instructors/{{admin_instructor_id}}/avatar
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file()
}
