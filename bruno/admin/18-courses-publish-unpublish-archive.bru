meta {
  name: Courses — publish / unpublish / archive
  type: http
  seq: 18
}

post {
  url: {{base_url}}/admin/courses/{{instructor_course_id}}/publish
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
