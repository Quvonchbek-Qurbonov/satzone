meta {
  name: Users — promote to instructor
  type: http
  seq: 5
}

post {
  url: {{base_url}}/admin/users/promote-instructor
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "email": "{{user_email}}"
  }
}
