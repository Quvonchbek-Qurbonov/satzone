meta {
  name: Instructors — patch
  type: http
  seq: 13
}

patch {
  url: {{base_url}}/admin/instructors/{{admin_instructor_id}}
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Senior SAT Coach"
  }
}
