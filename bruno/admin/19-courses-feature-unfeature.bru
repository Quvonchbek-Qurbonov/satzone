meta {
  name: Courses — feature / unfeature
  type: http
  seq: 19
}

post {
  url: {{base_url}}/admin/courses/{{instructor_course_id}}/feature
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
