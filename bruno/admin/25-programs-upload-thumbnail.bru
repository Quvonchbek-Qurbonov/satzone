meta {
  name: Programs — upload thumbnail
  type: http
  seq: 25
}

post {
  url: {{base_url}}/admin/programs/{{admin_program_id}}/thumbnail
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file()
}
