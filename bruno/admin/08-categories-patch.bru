meta {
  name: Categories — patch
  type: http
  seq: 8
}

patch {
  url: {{base_url}}/admin/categories/{{admin_category_id}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "description": "Updated"
  }
}
