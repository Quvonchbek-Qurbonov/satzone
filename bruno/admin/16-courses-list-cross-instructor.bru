meta {
  name: Courses — list (cross-instructor)
  type: http
  seq: 16
}

get {
  url: {{base_url}}/admin/courses
  auth: bearer
}

query {
  page: 1
  size: 20
}

auth:bearer {
  token: {{access_token}}
}
