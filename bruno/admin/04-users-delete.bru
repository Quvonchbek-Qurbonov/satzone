meta {
  name: Users — delete
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/admin/users/:user_id
  body: none
  auth: bearer
}

params:path {
  user_id: 
}

auth:bearer {
  token: {{access_token}}
}
