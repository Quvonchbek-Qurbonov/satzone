meta {
  name: Upload my avatar
  type: http
  seq: 4
}

post {
  url: {{base_url}}/instructor/me/profile/avatar
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file()
}
