meta {
  name: Update section
  type: http
  seq: 18
}

patch {
  url: {{base_url}}/instructor/sections/{{section_id}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "title": "Intro (renamed)"
  }
}
