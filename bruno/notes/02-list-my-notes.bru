meta {
  name: List my notes
  type: http
  seq: 2
}

get {
  url: {{base_url}}/me/notes
  auth: bearer
}

query {
  lesson_id: {{lesson_id}}
  ~course_id: {{course_id}}
}

auth:bearer {
  token: {{access_token}}
}
