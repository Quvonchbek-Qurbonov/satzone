meta {
  name: Instructor: delete item
  type: http
  seq: 13
}

delete {
  url: {{base_url}}/instructor/practice/items/{{practice_item_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
