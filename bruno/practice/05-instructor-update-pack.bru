meta {
  name: Instructor: update pack
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/instructor/courses/{{instructor_course_id}}/practice
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "title": "Vocabulary drills",
    "description": "Bite-sized practice to lock in this week's lessons."
  }
}
