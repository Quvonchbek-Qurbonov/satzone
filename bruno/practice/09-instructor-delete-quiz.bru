meta {
  name: Instructor: delete quiz
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/instructor/practice/quizzes/{{practice_quiz_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
