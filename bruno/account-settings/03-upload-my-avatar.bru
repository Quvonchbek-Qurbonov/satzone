meta {
  name: Upload my avatar
  type: http
  seq: 3
}

post {
  url: {{base_url}}/me/avatar
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file()
}
