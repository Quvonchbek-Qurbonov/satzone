meta {
  name: Password — forgot
  type: http
  seq: 10
}

post {
  url: {{base_url}}/auth/password/forgot
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{user_email}}"
  }
}
