meta {
  name: Password — reset
  type: http
  seq: 11
}

post {
  url: {{base_url}}/auth/password/reset
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{reset_token}}",
    "new_password": "BrandNewPass123!"
  }
}
