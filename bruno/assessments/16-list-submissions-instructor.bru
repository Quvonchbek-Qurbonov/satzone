meta {
  name: List submissions (instructor)
  type: http
  seq: 16
}

get {
  url: {{base_url}}/instructor/assessments/{{assessment_id}}/submissions
  auth: bearer
}

query {
  page: 1
  size: 20
}

auth:bearer {
  token: {{access_token}}
}
