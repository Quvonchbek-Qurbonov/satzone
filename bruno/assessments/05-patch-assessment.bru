meta {
  name: Patch assessment
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/instructor/assessments/{{assessment_id}}
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "status": "published"
  }
}
