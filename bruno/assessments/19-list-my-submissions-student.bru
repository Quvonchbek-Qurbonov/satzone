meta {
  name: List my submissions (student)
  type: http
  seq: 19
}

get {
  url: {{base_url}}/assessments/{{assessment_id}}/submissions/me
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
