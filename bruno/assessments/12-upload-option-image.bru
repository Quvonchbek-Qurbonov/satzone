meta {
  name: Upload option image
  type: http
  seq: 12
}

post {
  url: {{base_url}}/instructor/options/{{option_id}}/image
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  file: @file()
}
